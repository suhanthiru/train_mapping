# Extra deps for the offline trainer (train_eta.py) only — not needed to serve.
# ONNX export was planned but `onnx` won't install on Store-Python (Windows
# MAX_PATH); we export native XGBoost instead, so onnx/onnxmltools are omitted.
# Pinned to the verified versions (2026-07-12).
xgboost==3.2.0
polars==1.42.1
numpy==2.4.6
scikit-learn==1.9.0
